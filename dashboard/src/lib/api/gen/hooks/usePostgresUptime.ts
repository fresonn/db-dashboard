/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '@/lib/api/http-client.ts'
import type { PostgresUptimeQueryResponse, PostgresUptime400 } from '../models.ts'
import type { RequestConfig, ResponseErrorConfig } from '@/lib/api/http-client.ts'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult
} from '@tanstack/react-query'
import { postgresUptime } from '../clients/postgresUptime.ts'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const postgresUptimeQueryKey = () => [{ url: '/cluster/uptime' }] as const

export type PostgresUptimeQueryKey = ReturnType<typeof postgresUptimeQueryKey>

export function postgresUptimeQueryOptions(
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = postgresUptimeQueryKey()
  return queryOptions<
    PostgresUptimeQueryResponse,
    ResponseErrorConfig<PostgresUptime400>,
    PostgresUptimeQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal
      return postgresUptime(config)
    }
  })
}

/**
 * @description Get Postgres uptime info
 * @summary Get Postgres uptime info
 * {@link /cluster/uptime}
 */
export function usePostgresUptime<
  TData = PostgresUptimeQueryResponse,
  TQueryData = PostgresUptimeQueryResponse,
  TQueryKey extends QueryKey = PostgresUptimeQueryKey
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        PostgresUptimeQueryResponse,
        ResponseErrorConfig<PostgresUptime400>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: typeof fetch }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...queryOptions } = queryConfig
  const queryKey = queryOptions?.queryKey ?? postgresUptimeQueryKey()

  const query = useQuery(
    {
      ...postgresUptimeQueryOptions(config),
      queryKey,
      ...queryOptions
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<PostgresUptime400>> & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}
