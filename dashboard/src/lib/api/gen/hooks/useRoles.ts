/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { RolesQueryResponse, Roles400 } from '../models.ts'
import type { Client, RequestConfig, ResponseErrorConfig } from '@/lib/api/http-client.ts'
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult
} from '@tanstack/react-query'
import { roles } from '../clients/roles.ts'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const rolesQueryKey = () => [{ url: '/cluster/roles' }] as const

export type RolesQueryKey = ReturnType<typeof rolesQueryKey>

export function rolesQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
  const queryKey = rolesQueryKey()
  return queryOptions<
    RolesQueryResponse,
    ResponseErrorConfig<Roles400>,
    RolesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return roles({ ...config, signal: config.signal ?? signal })
    }
  })
}

/**
 * @description Returns a list of all available roles with their details
 * @summary Get available roles
 * {@link /cluster/roles}
 */
export function useRoles<
  TData = RolesQueryResponse,
  TQueryData = RolesQueryResponse,
  TQueryKey extends QueryKey = RolesQueryKey
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        RolesQueryResponse,
        ResponseErrorConfig<Roles400>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? rolesQueryKey()

  const query = useQuery(
    {
      ...rolesQueryOptions(config),
      ...resolvedOptions,
      queryKey
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<Roles400>> & { queryKey: TQueryKey }

  query.queryKey = queryKey as TQueryKey

  return query
}
